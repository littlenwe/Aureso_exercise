<%= form_for(@item_tag) do |f| %>
  <% if @item_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_tag.errors.count, "error") %> prohibited this item_tag from being saved:</h2>

      <ul>
      <% @item_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
